apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: LoadBalancer
  loadBalancerIP: 35.241.173.64
  ports:
    - port: 80
      protocol: TCP
      targetPort: 4040
  selector:
    app: api-deployment